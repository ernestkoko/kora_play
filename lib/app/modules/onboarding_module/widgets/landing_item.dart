import 'package:flutter/material.dart';

class LandingItem extends StatelessWidget {
  const LandingItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
